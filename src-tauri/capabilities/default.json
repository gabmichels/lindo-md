{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Least-privilege surface for the pretty-md window. The webview gets no filesystem or shell access: reading files, scanning folders, watching for changes and writing exports all go through allowlisted #[tauri::command] functions in Rust. Window permissions are present because the window is frameless and the titlebar is drawn by us.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:default",
    "core:window:allow-start-dragging",
    "core:window:allow-start-resize-dragging",
    "core:window:allow-minimize",
    "core:window:allow-toggle-maximize",
    "core:window:allow-is-maximized",
    "core:window:allow-close",
    "core:webview:allow-print",
    "dialog:allow-open",
    "dialog:allow-save",
    "dialog:allow-message",
    "opener:allow-open-url",
    "opener:allow-reveal-item-in-dir"
  ]
}
